services:
  app:
    build: .
    ports:
      - "3421:3421"
    volumes:
      - .:/app
      - /app/node_modules  # Exclude node_modules from bind mount
    environment:
      - PORT=3421
      - NEXT_TELEMETRY_DISABLED=1
    stdin_open: true
    tty: true
